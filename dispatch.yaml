application: dancebook-2016

dispatch:

  # Send all mobile traffic to the mobile frontend.
  - url: "*/mobile/*"
    module: mobile-frontend

  # Default module handles the rest.
  - url: "/*"
    module: default

